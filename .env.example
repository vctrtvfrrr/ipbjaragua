# Connection string for the app's dedicated Postgres database on the shared VPS.
# Rendered into .env by the CodeLab deploy-stack from the Ansible Vault at deploy time.
DATABASE_URL=postgres://user:password@host:5432/ipbjaragua
